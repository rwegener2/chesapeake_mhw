name: chesapeake_mhw
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (dont remove)
  - pytest
  # Examples (remove and add as needed)
  - jupyter
  - jupyterlab
  - xarray
  - numpy
  - scipy
  - dask
  - netcdf4
  - zarr
  - requests
  - aiohttp
  - pandas
  - matplotlib
  - seaborn
  - cartopy
